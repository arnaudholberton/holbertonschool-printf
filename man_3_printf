.TH _PRINTF 1 "27 Nov 2025" "1.0" "Holberton School"
.SH NAME
_printf \- prints formatted output to the standard output

.SH SYNOPSIS
To use the function, include the following header:
.B #include "main.h"
.P
The prototype of the function is:
.B int _printf(const char *format, ...);

.SH DESCRIPTION
The
.B _printf
function prints text to the standard output (stdout) according to a given format.

The format is a string that may contain ordinary characters (printed as-is) or **conversion specifiers**, which begin with '%'.

The following conversion specifiers are supported in this version:

.TP
.B c
Prints a single character.

.TP
.B s
Prints a string. If the string is NULL, it prints "(null)".

.TP
.B %
Prints the '%' character.

.TP
.B d, i
Prints a signed integer in base 10.

⚠️ Flags, field width, precision, and length modifiers are **not supported** in this version.

.SH RETURN VALUE
The function returns the **number of characters printed** (excluding the terminating null byte).  
If the format is NULL, it returns -1.

.SH EXAMPLES
.B _printf("Hello %s!\n", "World");
Output: Hello World!

.B _printf("Number: %d\n", 42);
Output: Number: 42

.B _printf("Percent: %%\n");
Output: Percent: %

.SH DATE
25 November 2025

.SH AUTHORS
James Roussel and Arnaud Pemjean
