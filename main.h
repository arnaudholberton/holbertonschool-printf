#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>

/* Prototype of the _printf function */
int _printf(const char *format, ...);

/* Utility functions you may use */
int _putchar(char c);
int _puts(char *str);

#endif /* MAIN_H */

